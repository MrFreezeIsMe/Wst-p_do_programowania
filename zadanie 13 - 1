#include <iostream>
#include <vector>

using namespace std;

void printVector(vector<int> wektor)
{
	for (int i = 0; i < wektor.size(); i++)
	{
		cout << wektor.at(i) << " ";
	}
}

int main()
{
	vector<int> wektor = { -3,4,5 };
	printVector(wektor);
}
