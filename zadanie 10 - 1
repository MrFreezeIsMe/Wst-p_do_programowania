#include <iostream>

using namespace std;

int main()
{
    int value;
    int sum=0;

    cout << "Insert value of n: \n";
    cin >> value;
    while (value > 0)
    {
        sum = sum + value;
        value--;
    }

    cout << sum;
}
