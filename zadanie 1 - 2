#include <iostream>

using namespace std;

int main()
{
    for (int i = 0; i < 25; i++)
    {
        cout << (i + 1) * 2 << ", ";
    }
    int i = 25;

    while (i >= 25 && i < 50)
    {
        if (i < 49)
        {
            cout << (i + 1) * 2 << ", ";
            i++;
        }
        else
        {
            cout << (i + 1) * 2;
            i++;
        }
    }
}

